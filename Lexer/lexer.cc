#include "lexer.h"

int Lexer::compile(const std::vector<std::string> > &regex_rules, const std::vector<int> &rule_ids) {
  
  return 0;
}

int Lexer::next_token(const char *text, int start_pos, int *rule_id, int *end_pos) {
  return 0;
}
